# zine-log

一个首版图文排版设计工具网页，支持上传多张图片、填写标题与短文案，并套用模板生成设计稿。

## 本地启动

直接打开 `index.html` 即可预览，或者在仓库根目录运行：

```bash
python3 -m http.server 8000
```

然后访问 `http://localhost:8000`。

## 当前版本

- 4 个模板：L 型回纹、留白日记、拼贴栏位、照片环
- 支持多张图片上传与顺序排版
- 实时预览
- 本地导出 PNG / SVG / PDF（高分辨率栅格导出）
